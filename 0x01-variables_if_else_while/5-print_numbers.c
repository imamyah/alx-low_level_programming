#include <stdio.h>
/**
 * main - prints all single digit numbers of base
 * starting from 0 to 10, followed with a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d\n", n);
	}
	return (0);
}
