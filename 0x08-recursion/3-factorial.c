#include "main.h"

/**
 * factorial - returns the factorial of a given number
 * @n: input value
 *
 * Return: the factoriel of n
 */

int factorial(int n)
{
	if (n = factorial(0))
		return (1);

	if (n < 0)
		return (-1);

	return (n = n * factorial(n - 1));
}
