#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	char c;
	c = a * b;
	_putchar(c);
	return (0);
}
